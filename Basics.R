# My first R Program for 4th Semester
print("Hello R Programming!")

# Creating a simple sequence of numbers
numbers <- c(10, 20, 30, 40, 50)
print(numbers)

# Calculating the Mean (Average)
result <- mean(numbers)
print(paste("The average is:", result))
